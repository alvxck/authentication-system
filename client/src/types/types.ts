
export type registrationCredentials = {
    event: React.FormEvent, 
    name: string, 
    email: string, 
    password: string,
}

export type loginCredentials = {
    event: React.FormEvent, 
    email: string, 
    password: string,
}